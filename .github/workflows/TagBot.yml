name: TagBot
on:
  schedule:
    - cron: 0 * * * *
jobs:
  TagBot:
    runs-on: ubuntu-latest
    steps:
      - uses: JuliaRegistries/TagBot@cdg/dev
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
